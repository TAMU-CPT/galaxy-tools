<?xml version="1.0"?>
<tool id="edu.tamu.cpt.gff3.export_seq" name="GFF3 Feature Sequence Export" version="1.2">
  <description>Export corresponding sequence in genome from GFF3</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <version_command>python gff3_extract_sequence.py --version</version_command>
  <command detect_errors="aggressive"><![CDATA[
@GENOME_SELECTOR_PRE@

$__tool_directory__/gff3_extract_sequence.py
@GENOME_SELECTOR@

$gff3_data

#if $feature_filter and $feature_filter is not None:
--feature_filter $feature_filter
#end if
$nodesc
> $default]]></command>
  <inputs>
      <expand macro="genome_selector" />
      <expand macro="gff3_input" />
    <param label="Filter for specific feature types (--feature_filter)" name="feature_filter" type="text"/>
    <param label="Remove description (use if blasting)" name="nodesc" type="boolean" truevalue="--nodesc" falsevalue=""/>
  </inputs>
  <outputs>
    <data format="fasta" hidden="false" name="default"/>
  </outputs>
  <help><![CDATA[
Extract sequences from a parent genome.

*Hint*: Use "nice_cds" as the feature type for WebApollo generated features.
      ]]></help>
</tool>
