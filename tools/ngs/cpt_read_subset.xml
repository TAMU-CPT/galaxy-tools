<?xml version="1.0"?>
<tool id="edu.tamu.cpt.assembly.coverage_calc" name="FastQ Subset" version="1.0">
  <description>reduce results for X-fold coverage</description>
  <stdio>
    <exit_code level="fatal" range="1:"/>
  </stdio>
  <command interperter="bash">cpt_read_subset.sh "${file}" "${length}" "${coverage}" > $fastq</command>
  <inputs>
    <param help="FastQ Readset" label="file" name="file" optional="False" type="data" format="fastq,fastqsanger"/>
    <param help="Expected length of genome" label="length" name="length" optional="False" type="integer" value="100000"/>
    <param help="Desired coverage" label="coverage" name="coverage" optional="False" type="integer" value="100"/>
  </inputs>
  <outputs>
    <data format="fastq" name="fastq"></data>
  </outputs>
  <help>Subset FastQ file
=================

Reduce a FastQ file to a specific fold coverage. In phage genomics, we've
started seeing huge coverage numbers for genome sequences (>500X). These seem
to be leading to poor assemblies and mis-assemblies. We've found that reducing
the coverage to ~100X produce better assemblies.
</help>
</tool>
