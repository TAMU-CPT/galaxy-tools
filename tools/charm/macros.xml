<macros>
    <macro name="dummy_input">
        <param
            label="Unused, dummy ionput"
            name="dummy"
            type="data"
            multiple="true"
            help="The GMOD tools often don't transmit an output to another input, so this dummy input allows you to maintain correct execution order. E.g. the output of adding an organism's output isn't used anywhere, but absolutely must be executed before data is loaded."
            />
    </macro>
    <macro name="input_gff3">
        <param
            label="GFF3 file to process"
            name="gff"
            type="data"
            />
    </macro>
    <macro name="output_gff3">
        <data
            format="gff3"
            name="default"
            />
    </macro>
</macros>
