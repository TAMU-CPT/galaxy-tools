<?xml version="1.0"?>
<tool id="PhageInfo" name="Genbank Genome Information" version="1.94">
  <description>calculates various statistics and information about a genbank file</description>
  <version_command>perl bin/cpt_phagestats.pl --version</version_command>
  <stdio>
    <exit_code range="1:" level="fatal"/>
  </stdio>
  <command interpreter="perl">bin/cpt_phagestats.pl
--galaxy
--outfile_supporting $__new_file_path__
--file "${file}"

--results "${results}"

--results_files_path "${results.files_path}"

--results_format "${results_format}"

--results_id "${results.id}"

</command>
  <inputs>
    <param name="file" help="Input file" optional="False" label="file" type="data"/>
    <param name="results_format" help="Genbank Info and Statistics" optional="False" label="Format of results" type="select">
      <option value="HTML" selected="True">HTML</option>
    </param>
  </inputs>
  <outputs>
    <data name="results" format="HTML">
      <change_format>
        <when input="results_format" value="HTML" format="html"/>
      </change_format>
    </data>
  </outputs>
  <help>DESCRIPTION
===========

Genomic Info, very similar to the "Genome Overview" in artemis.
</help>
  <tests/>
</tool>
