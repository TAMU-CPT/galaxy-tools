<?xml version="1.0"?>
<tool id="FixGeneBoundaries" name="Fix Gene Boundaries" version="1.94">
  <description>to include RBS based on locus_tag</description>
  <version_command>perl bin/cpt_fix_gene_boundaries.pl --version</version_command>
  <stdio>
    <exit_code range="1:" level="fatal"/>
  </stdio>
  <command interpreter="perl">bin/cpt_fix_gene_boundaries.pl
--galaxy
--outfile_supporting $__new_file_path__
--file "${file}"

--genbank "${genbank}"

--genbank_files_path "${genbank.files_path}"

--genbank_format "${genbank_format}"

--genbank_id "${genbank.id}"

</command>
  <inputs>
    <param name="file" help="Input file" optional="False" label="file" type="data"/>
    <param name="genbank_format" help="GBK with Fixed Boundaries" optional="False" label="Format of genbank" type="select">
      <option value="Genbank" selected="True">Genbank</option>
    </param>
  </inputs>
  <outputs>
    <data name="genbank" format="Genbank">
      <change_format>
        <when input="genbank_format" value="Genbank" format="txt"/>
      </change_format>
    </data>
  </outputs>
  <help>DESCRIPTION
===========

Some organisations expect gene boundaries to include RBSs and CDSs. In
order to accomodate them, this tool modifies the boundaries of ``gene``
features to include both relevant subfeatures. This is accomplished
through use of the locus tag, so it MUST be identical between CDS, RBS,
and gene.
</help>
  <tests/>
</tool>
