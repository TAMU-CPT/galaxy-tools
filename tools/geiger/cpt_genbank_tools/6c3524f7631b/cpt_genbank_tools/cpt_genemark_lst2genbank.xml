<?xml version="1.0"?>
<tool id="GenemarkFastaMergeLST" name="GenBank from GeneMarkS LST output" version="1.94">
  <description>creates a valid genbank file from GeneMarkS and Fasta</description>
  <version_command>perl bin/cpt_genemark_lst2genbank.pl --version</version_command>
  <stdio>
    <exit_code range="1:" level="fatal"/>
  </stdio>
  <command interpreter="perl">bin/cpt_genemark_lst2genbank.pl
--galaxy
--outfile_supporting $__new_file_path__
--file_fasta "${file_fasta}"

--file_lst "${file_lst}"

--genemark "${genemark}"

--genemark_files_path "${genemark.files_path}"

--genemark_format "${genemark_format}"

--genemark_id "${genemark.id}"

</command>
  <inputs>
    <param format="fasta" name="file_fasta" help="Input Fasta file" optional="False" label="file_fasta" type="data"/>
    <param name="file_lst" help="Input genemark LST formatted calls" optional="False" label="file_lst" type="data"/>
    <param name="genemark_format" help="Merged Genbank File" optional="False" label="Format of genemark" type="select">
      <option value="Genbank" selected="True">Genbank</option>
    </param>
  </inputs>
  <outputs>
    <data name="genemark" format="Genbank">
      <change_format>
        <when input="genemark_format" value="Genbank" format="txt"/>
      </change_format>
    </data>
  </outputs>
  <help>DESCRIPTION
===========

This tool depends on the output of GeneMarkS in LST format. With the LST
formatted GeneMarkS output and a fasta sequence, it will produce a
genbank file ready for annotation. The genbank file lacks any feature
IDs, so you might want to use the CPT's gene renumbering tool to add
those.
</help>
  <tests/>
</tool>
