<?xml version="1.0"?>
<tool id="Root" name="Root" version="1.94.1">
  <description>ferrets out original annotation of a given protein</description>
  <version_command>perl bin/cpt_root.pl --version</version_command>
  <stdio>
    <exit_code range="1:" level="fatal"/>
  </stdio>
  <command interpreter="perl">bin/cpt_root.pl
--galaxy
--outfile_supporting $__new_file_path__
#if $protein and $protein is not "None":
--protein "${protein}"
#end if

--name "${name}"

--results "${results}"

--results_files_path "${results.files_path}"

--results_format "${results_format}"

--results_id "${results.id}"

</command>
  <inputs>
    <param name="protein" help="Input file containing a single protein sequence" optional="True" label="protein" type="data"/>
    <param name="name" help="Name of the protein" optional="False" label="name" type="text"/>
    <param name="results_format" help="Root Results" optional="False" label="Format of results" type="select">
      <option value="HTML" selected="True">HTML</option>
    </param>
  </inputs>
  <outputs>
    <data name="results" format="HTML">
      <change_format>
        <when input="results_format" value="HTML" format="html"/>
      </change_format>
    </data>
  </outputs>
  <help>DESCRIPTION
===========

This tool attempts to find the first instance of a protein being
annotated as "X", where X is your search query. If you were interested
in the first time someone annotated a "Large Terminase" as a "Large
Terminase", you would query on that. It is recommended you add an actual
terminase sequence so as to identify which annotations are "true"
annotations to your protein of interest and not misannotation by other
researchers.
</help>
  <tests/>
</tool>
